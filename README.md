# VL - Perfume (React)

This is a React + Vite recreation of [`https://vyvo.life/`](https://vyvo.life/) 

## Preview locally

From the `vyvo` folder:

```bash
npm install
npm run dev
```

Open:

- `http://localhost:5175`

